import { createApp } from 'vue'
import App from './App.vue'

// Create Vue.js application
const app = createApp(App)

// Mount the app
app.mount('#app')

console.log('Vue.js application started successfully!')
